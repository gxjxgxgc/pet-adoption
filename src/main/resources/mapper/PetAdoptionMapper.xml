<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petadoption.petadoption.mapper.PetAdoptionMapper">

    <resultMap id="BaseResultMap" type="PetAdoption">
        <id column="id" property="id"/>
        <result column="pet_id" property="petId"/>
        <result column="applicant_id" property="applicantId"/>
        <result column="message" property="message"/>
        <result column="status" property="status"/>
        <result column="reason" property="reason"/>
        <result column="audit_time" property="auditTime"/>
        <result column="auditor_id" property="auditorId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectByApplicantId" resultMap="BaseResultMap">
        SELECT * FROM pet_adoption WHERE applicant_id = #{applicantId} AND deleted = 0 ORDER BY create_time DESC
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT * FROM pet_adoption WHERE status = #{status} AND deleted = 0 ORDER BY create_time DESC
    </select>

    <select id="selectByPetId" resultMap="BaseResultMap">
        SELECT * FROM pet_adoption WHERE pet_id = #{petId} AND deleted = 0
    </select>

</mapper>
