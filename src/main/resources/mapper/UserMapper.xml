<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petadoption.petadoption.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="real_name" property="realName"/>
        <result column="avatar" property="avatar"/>
        <result column="gender" property="gender"/>
        <result column="age" property="age"/>
        <result column="birthday" property="birthday"/>
        <result column="id_card" property="idCard"/>
        <result column="is_verified" property="isVerified"/>
        <result column="user_type" property="userType"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="bio" property="bio"/>
        <result column="address" property="address"/>
        <result column="credit_score" property="creditScore"/>
        <result column="favorite_count" property="favoriteCount"/>
        <result column="adoption_count" property="adoptionCount"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM pet_user WHERE username = #{username} AND deleted = 0
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) > 0 FROM pet_user WHERE username = #{username} AND deleted = 0
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0 FROM pet_user WHERE email = #{email} AND deleted = 0
    </select>

    <select id="existsByPhone" resultType="boolean">
        SELECT COUNT(*) > 0 FROM pet_user WHERE phone = #{phone} AND deleted = 0
    </select>

</mapper>
